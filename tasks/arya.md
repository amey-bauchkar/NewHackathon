# Arya's Tasks

**Role**: Frontend (UI & Layout)
**Assigned Directories**: `src/app/`, `src/components/ui/`, `src/components/views/`

## Instructions
Arya is responsible for creating core reusable UI components and assembling them into responsive pages. Arya will use Tanmay's custom hooks and Tanish's forms to build the final page layouts.

## Task List
- [ ] Develop core UI components (Buttons, Cards, Badges)
- [ ] Setup main page layouts and routing in `src/app/`
- [ ] Assemble feature-specific views in `src/components/views/`
